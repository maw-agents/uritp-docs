# Build dependencies for the docs site.
# Also the file actions/setup-python hashes for its pip cache.

# 9.7 (Nov 2025) released every former Insiders feature to everyone and put the
# project into maintenance mode.
#
# ~~`material.extensions.preview` (instant previews, mkdocs.yml) DOES NOT EXIST
# below 9.7, and an unknown markdown extension is a hard build failure, so this
# floor is load-bearing rather than housekeeping.~~ The extension was REMOVED
# 2026-08-01 (~~it iconised every nav row for a feature phones cannot use~~ --
# ⚠️ HALF WRONG, corrected same day: it never iconised anything. Those were
# Material's own `status:` badges reading our frontmatter, and they outlived the
# removal. It went for the hover-on-a-phone reason alone), so the floor is no
# longer load-bearing. Struck rather than deleted: the next person reaching for
# a former-Insiders feature needs to know why 9.7 is the number.
#
# ⚠️ KEEP THE FLOOR ANYWAY, and now for a second reason: the `status:` badge in
# partials/nav-item.html is Material behaviour we are actively suppressing in
# uritp.css. Moving off 9.x changes that template. See mkdocs.yml.
mkdocs-material>=9.7,<10
mkdocs-awesome-nav>=3.3,<4
cryptography>=42                 # AES-GCM for hooks/visibility.py (status: gated)
